{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "npm run preview --workspace=frontend -- --host 0.0.0.0 --port $PORT"
  }
}
